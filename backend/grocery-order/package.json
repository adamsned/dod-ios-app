{
  "name": "dod-grocery-order",
  "version": "1.0.0",
  "private": true,
  "description": "Grocery order Worker (DUT-532) — turns a recipe ingredient list into a one-tap order link. Instacart (live) + Walmart search fallback.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
