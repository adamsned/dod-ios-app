{
  "name": "dod-siwa-revoke",
  "version": "1.0.0",
  "private": true,
  "description": "Sign in with Apple token-revoke Worker (DUT-98) — code↔refresh exchange + /auth/revoke for App Store 5.1.1(v).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
